# Copy to .env for local development (Vercel uses Project Settings -> Environment Variables)

# Gmail SMTP (recommended: App Password)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=youraddress@gmail.com
SMTP_PASS=your_gmail_app_password

# Where to send contact messages
MAIL_TO=info@lertisento.fi

# From header shown in email.
# Tip: for Gmail, keep it the same as SMTP_USER to avoid DMARC/SPF issues.
MAIL_FROM="Lertisento Website" <youraddress@gmail.com>

# Optional (CORS). Usually not required when calling same origin on Vercel.
# CORS_ORIGIN=https://your-project.vercel.app
